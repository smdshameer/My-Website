{
  "name": "hi-secure-solutions-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w apps/web build && npm run -w apps/server build",
    "dev": "npm run -w apps/server dev & npm run -w apps/web dev",
    "typecheck": "npm run -w apps/web typecheck && npm run -w apps/server typecheck"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  }
}
